# CTS payload manifest — paths copied into target projects by cts-sync.sh.
# One path per line. Trailing "/" marks a directory (copied recursively).
# Lines starting with "#" and blank lines are ignored.

AGENTS.md
CLAUDE.md
.mcp.json
rules/
docs/RECIPES.md
docs/METRICS.md
# METRICS.md IS shipped (unlike KNOWLEDGE_INBOX.md) to seed the header/format at
# init; cts-sync.sh's is_locally_modified() skips it once a repo appends real rows.
.claude/agents/
.claude/skills/
.claude/scripts/
.claude/hooks/
.claude/settings.json

# Explicitly NOT payload: README.md, CHANGELOG.md, THIRD_PARTY.md, LICENSE,
# images, tasks/, tmp/, cts-payload.txt itself, .claude/settings.local.json,
# docs/KNOWLEDGE_INBOX.md (project-local, never synced to consumers),
# .claude/skills/cts-review-contribution/ (present in this repo, excluded
# from the .claude/skills/ payload entry by OWNER_ONLY_SKILLS in
# cts-sync.sh — CTS-owner tool, never synced to consumers).
